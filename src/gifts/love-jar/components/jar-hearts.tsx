'use client';

import type { Heart } from '../hooks/use-jar-state';

interface JarHeartsProps {
  hearts: Heart[];
  intensity: number;
}

export function JarHearts({ hearts, intensity }: JarHeartsProps) {
  return null;
}
